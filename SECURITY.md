# 安全策略

## 支持范围

仅维护默认分支的最新版本。项目是本地工具，服务必须保持监听 `127.0.0.1`，不应直接暴露到公网。

## 报告问题

请使用 GitHub 仓库的 **Security → Report a vulnerability** 私密报告安全问题。不要在普通 Issue 中粘贴 Cookie、访问令牌、浏览器配置、签名链接、采集内容或本机路径。

## 敏感文件

下列内容由 `.gitignore` 排除，不得强制提交：

- `runtime/` 与 `chrome_profile/` 浏览器会话；
- `parameter.txt` 本机旧配置与签名链接；
- `Information/` 采集内容和导出报告；
- `.env*`、虚拟环境、缓存及日志。

如果敏感信息曾进入 Git 历史，仅删除当前文件并不够；应立即轮换相关凭据，并清理完整历史后再共享仓库。

